.modal-dialog(role='document')
  .modal-content.h-open-annotated-image-dialog
    .modal-header
      button.close(type='button', data-dismiss='modal', aria-label='Close')
        span(aria-hidden='true') &times;
      h4.modal-title Chọn hình ảnh đã chú thích gần đây để mở
    .modal-body
      form.form-inline
        .form-group
          label(for='h-image-name') Tên hình ảnh
          input#h-image-name.form-control(type='text', value=imageName)
        .form-group
          label(for='h-annotation-creator') Người tạo
          select#h-annotation-creator.form-control(type='text')
            option(value='', selected= !creator) Bất kỳ người dùng nào
            for user in users.models
              option(value=user.id, selected= creator === user.id)
                = user.get('login')
      hr
      .h-annotated-images-list-container
